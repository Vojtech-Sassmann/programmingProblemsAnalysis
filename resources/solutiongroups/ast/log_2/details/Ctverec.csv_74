1;root{Module{Import{alias{}, }, FunctionDef{For{Call{}, Assign{Str{}, }, For{Call{}, If{BoolOp{Or{}, Compare{Sub{}, }, Compare{Sub{}, }, }, Assign{Add{}, Str{}, }, Expr{}, }, }, }, }, }, }
import sys

def ctverec(n):
    for y in range(n):
        row = ""
        for x in range(n):
            if  x == 0 or y == 0 or y == (n-1) or x == (n-1):
                row = row + "*"
            else:
                row 
