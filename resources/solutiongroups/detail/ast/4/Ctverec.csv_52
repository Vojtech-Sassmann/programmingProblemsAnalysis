1;root{Module{FunctionDef{For{Call{}, Assign{Str{}, }, If{BoolOp{Or{}, Compare{Sub{}, }, }, Print{Mult{}, }, Assign{Add{}, Sub{}, Mult{}, Str{}, Add{}, }, Print{}, }, }, }, }, }
def ctverec(n):
    for i in range(n):
        a = "*"
        if i == 0 or i == (n-1):
            print n*a
        else:
            a = a + (n-2)*"+" + a
            print a 
