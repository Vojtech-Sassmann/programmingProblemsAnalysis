1;root{Module{FunctionDef{If{Return{}, If{Compare{Gt{}, }, Assign{Sub{}, }, Return{Call{}, }, Assign{Sub{}, }, Return{Call{}, }, }, }, }, }, }
def nsd(a, b):
    if a == b:
        return a
    else:
        if a > b:
            a = a - b
            return nsd(a,b)
        else: 
            b = b - a
            return nsd(a,b)


 
