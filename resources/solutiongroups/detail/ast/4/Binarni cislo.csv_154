1;root{Module{FunctionDef{Assign{}, For{Call{Call{}, }, Assign{Call{}, }, Assign{Add{}, Mult{}, Pow{}, Call{Call{}, Sub{}, Sub{}, }, }, }, Return{}, }, }, }
def vyhodnot(r):
    desitkova = 0
    for i in range(len(r)):
        c = int(r[i])
        desitkova = desitkova + c*2**int(len(r)-1-i)
    return desitkova
        
 
