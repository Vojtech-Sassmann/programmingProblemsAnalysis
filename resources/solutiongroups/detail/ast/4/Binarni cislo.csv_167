1;root{Module{FunctionDef{Assign{}, For{Call{Call{}, }, Assign{}, Assign{Subscript{Slice{Call{}, Sub{}, }, }, }, If{Compare{IsNot{}, }, Return{}, }, If{Compare{Call{}, }, AugAssign{Add{}, Pow{}, }, }, Expr{}, }, }, }, }
def vyhodnot(retezec):
    summ = 0
    for i in range(len(retezec)):
        a = retezec[-1]
        retezec = retezec[:len(retezec)-1]
        if a is not int: return 0
        if int(a)==1:
            summ += 2**i
        el 
