1;root{Module{FunctionDef{For{Call{}, Assign{Str{}, }, For{Call{}, If{BoolOp{Or{}, Compare{Sub{}, }, Compare{Sub{}, }, }, Assign{Add{}, Call{Str{}, }, }, Assign{Add{}, Call{Str{}, }, }, }, }, }, }, }, }
def ctverec(n):
    for x in range(n):
        txt=""
        for y in range(n):
            if y==0 or y==(n-1)or x==0 or x==(n-1):
                txt=txt+str("*")
            else:
                txt=txt+str("+")
 
