2;root{Module{FunctionDef{Assign{Str{}, }, While{Compare{NotEq{}, }, If{Compare{Mod{}, }, Assign{Add{}, Str{}, }, Assign{Add{}, Str{}, }, }, Assign{FloorDiv{}, }, }, Print{}, }, }, }
def binarne(n):
    x=""
    while n!=0:
        if n%2==0:
            x=x+"0"
        else:
            x=x+"1"
        n=n//2
    print x

def binarne(n):
    a=""
    while n!=0:
        if n % 2 == 0: a=a+"0"
        else: a=a+"1"
        n=n//2
    print a[::-1]
 
