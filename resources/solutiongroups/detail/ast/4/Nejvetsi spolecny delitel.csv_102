1;root{Module{FunctionDef{Assign{Call{}, }, For{Call{Add{}, }, If{BoolOp{And{}, Compare{Mod{}, Sub{}, Add{}, }, Compare{Mod{}, Sub{}, Add{}, }, }, Return{Sub{}, Add{}, }, }, }, }, }, }
def nsd(a, b):
    m = min(a,b)
    for i in range(1,m+1):
        if a % (m - i + 1) == 0 and b % (m - i + 1) == 0:
            return m - i + 1
 
