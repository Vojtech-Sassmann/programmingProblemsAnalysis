2;root{Module{FunctionDef{Assign{}, For{Call{Call{}, }, If{Compare{Subscript{Index{Add{}, }, }, NotEq{}, Subscript{Index{Call{}, Sub{}, }, }, }, Assign{}, }, Return{}, }, }, }, }
def palindrom(text):
    p=True
    for i in range (1,len(text)):
        
        if text[0+i] != text[len(text)-i]:
            p=False
        return p
 
def palindrom(text):
    for i in range(len(text)):
        if text[0+i] != text[len(text)-1-i]:
            return False
        return True
 
