1;root{Module{FunctionDef{Assign{List{}, }, While{Compare{Gt{}, }, If{Compare{Mod{}, }, Expr{Call{Attribute{}, }, }, Expr{Call{Attribute{}, }, }, }, Assign{Div{}, }, }, For{Call{Call{}, Add{}, }, Print{Subscript{Index{UnaryOp{USub{}, }, }, }, }, }, }, }, }
def binarne(n):
    zoznam=[]
    while n>0:
        if n%2==0:
            zoznam.append(0)
        else:
            zoznam.append(1)
        n=n/2
    for i in range(1,len(zoznam)+1):
        print zoznam[-i],

