1;root{Module{FunctionDef{Assign{List{}, }, While{Compare{Gt{}, }, If{Compare{Mod{}, }, Expr{Call{Attribute{}, }, }, Expr{Call{Attribute{}, }, }, }, Assign{FloorDiv{}, }, }, Assign{Str{}, }, For{Subscript{Slice{}, }, AugAssign{Add{}, Call{}, }, }, Print{}, }, }, }
def binarne(n):
    result = []
    while (n > 0):
        if n % 2 == 0: result.append(0)
        else: result.append(1)
        n = n//2
    stringb = "" 
    for i in result[::-1]:
        stringb += str(i)
    print string 
