1;root{Module{FunctionDef{Assign{List{}, }, For{Call{Call{}, }, If{Compare{Call{}, }, Expr{Call{Attribute{}, }, }, }, }, Return{}, }, FunctionDef{Assign{Call{Attribute{}, Str{}, }, }, }, }, }
def vymazat_ostatne(slova, najdlhsie):
    najslova = []
    for i in range (len(slova)):
        if len(slova[i]) == najdlhsie: najslova.append(slova[i])
    return najslova

def nejdelsi_slovo(text):
    slova = text.split(" ")
 
