1;root{Module{FunctionDef{Assign{}, Assign{Call{}, Sub{}, }, For{Call{}, If{Compare{NotEq{}, Subscript{Index{Sub{}, }, }, }, Assign{Add{}, }, }, }, If{Return{}, Return{}, }, }, }, }
def palindrom(text):
    x=0
    dlzka=len(text)-1
    for i in range(dlzka):
        if text[i] != text[dlzka-i]:
            x=x+1
    if x==0: return True
    else: return False
 
