1;root{Module{FunctionDef{Assign{}, Assign{}, If{Compare{Lt{}, }, For{Call{Call{}, Div{}, }, If{Compare{NotEq{}, Subscript{Index{Call{}, Sub{}, }, }, }, Return{}, }, Assign{Add{}, }, Assign{Add{}, }, }, }, Return{}, }, }, }
def palindrom(text):
    x = 1
    s = 0
    if s < x:
       for i in range(len(text)/2):
        if text[s] != text[len(text)-x]:
            return False
        x =x+1
        s=s+1
    return True
 
