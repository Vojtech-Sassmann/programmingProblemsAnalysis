1;root{Module{FunctionDef{Assign{Call{Attribute{}, Str{}, }, }, Assign{List{}, }, For{Call{Call{}, }, If{Compare{Call{}, Gt{}, Call{}, }, Assign{List{}, }, If{Compare{Call{}, Call{}, }, Expr{}, }, }, }, }, }, }
def nejdelsi_slovo(text):
    slova = text.split(" ")
    orig = [slova[0]]
    for i in range(len(slova)):
        if len(slova[i])>len(orig[0]):
            orig = [slova[i]]
        elif len(slova[i])==len(orig[0]):
            o 
