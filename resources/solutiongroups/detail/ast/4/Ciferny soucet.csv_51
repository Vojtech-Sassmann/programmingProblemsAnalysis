1;root{Module{FunctionDef{Assign{Call{}, }, Assign{Call{}, }, Assign{Call{}, }, For{Call{}, AugAssign{Add{}, Call{Mod{}, }, }, Assign{Call{Div{}, }, }, }, Return{Call{}, }, }, }, }
def ciferny_soucet(n):
    soucet = int(0)
    y = str(n)
    x = len (y)
    for i in range (x):
        soucet += int(n % 10)
        n = int(n/10)
    return int (soucet)
 
