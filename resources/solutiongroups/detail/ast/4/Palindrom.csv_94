1;root{Module{FunctionDef{For{Call{Call{}, FloorDiv{}, Add{}, Call{}, Mod{}, Add{}, }, If{Compare{NotEq{}, Subscript{Index{Call{}, Sub{}, Sub{}, }, }, }, Return{}, }, }, Return{}, }, }, }
def palindrom(text):
    for i in range(len(text)//2+len(text)%2+1):
        if text[i] != text[len(text)-1-i]:
            return False
    return True
 
