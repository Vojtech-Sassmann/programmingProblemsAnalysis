1;root{Module{FunctionDef{Assign{}, Assign{}, For{Call{Call{}, }, If{BoolOp{Or{}, Compare{Subscript{Index{Call{}, Sub{}, Sub{}, }, }, Str{}, }, Compare{Subscript{Index{Call{}, Sub{}, Sub{}, }, }, Str{}, }, }, AugAssign{Add{}, Mult{}, Call{Subscript{Index{Call{}, Sub{}, Sub{}, }, }, }, }, }, }, }, }, }
def vyhodnot(retazec):
    mocnina=1
    hodnota=0
    for i in range(len(retazec)):
        if retazec[len(retazec)-1-i]=="1" or retazec[len(retazec)-1-i]=="0": 
            hodnota+=  mocnina*int(retazec[len(retazec)-1-i])
 
