1;root{Module{FunctionDef{If{Return{}, Return{Call{FloorDiv{}, }, Add{}, Call{Mod{}, }, }, }, }, Print{Call{}, }, }, }
def binarne(n):
    if n == 0:
        return 
    else:
        return binarne(n // 2) + str(n % 2)
print binarne(100)
