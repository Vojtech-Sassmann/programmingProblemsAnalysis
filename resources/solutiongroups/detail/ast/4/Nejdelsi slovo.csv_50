1;root{Module{FunctionDef{Assign{Call{Attribute{}, Str{}, }, }, Assign{List{}, }, For{Call{Call{}, }, Expr{Call{Attribute{}, Call{}, }, }, }, For{Call{}, Print{}, }, Return{}, }, }, }
def nejdelsi_slovo(text):
    slova = text.split(" ")
    b=[]
    for i in range(len(slova)):
         b.append(len(slova[i]))
    for j in range(b):
        print b[j]
    return slova[pomocka]
 
