1;root{Module{FunctionDef{Assign{Str{}, }, For{Call{Call{}, }, If{Compare{Str{}, }, AugAssign{Add{}, Str{}, }, AugAssign{Add{}, Call{Call{}, Add{}, }, }, }, }, Return{}, }, }, }
def caesar(text, n):
    novy=""
    for i in range(len(text)):
        if text[i]==" ":
            novy+= " "
        else:
            novy += chr(ord(text[i])+n)
    return novy
 
