1;root{Module{FunctionDef{For{If{Compare{Call{}, }, Return{}, }, }, Assign{Call{}, Sub{}, }, Assign{}, For{Assign{Call{}, Mult{}, Pow{}, Add{}, }, AugAssign{Sub{}, }, }, Expr{}, }, }, }
def vyhodnot(retezec):
    for i in (retezec):
        if type(i)==str:
            return 0
    mocnina=len(retezec)-1
    soucet=0
    for i in (retezec):
        soucet=(int(i)*2**mocnina)+soucet
        mocnina-=1
    retu 
